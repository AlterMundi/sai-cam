# Core dependencies
PyYAML>=6.0          # For config file handling
opencv-python>=4.8.0 # For camera operations (cv2 module)
requests>=2.28.0     # For HTTP operations
numpy>=1.24.0        # Required by OpenCV and image processing

# Camera protocols
onvif-zeep>=0.2.12   # For ONVIF camera support (onvif module)

# System monitoring
psutil>=5.9.0        # For system resource monitoring
watchdog>=3.0.0      # For filesystem monitoring

# Systemd integration (optional for testing)
systemd-python>=235  # For systemd watchdog support

# Status portal
flask>=3.0.0         # For web status portal

# Development and testing (optional)
# pytest>=7.0.0      # For unit testing
# black>=22.0.0      # For code formatting

