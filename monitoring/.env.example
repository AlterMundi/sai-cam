# VictoriaMetrics authentication
VM_AUTH_USER=admin
VM_AUTH_PASSWORD=changeme

# Grafana admin credentials
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=changeme

# Host ports (override to avoid collisions)
VM_PORT=8428
GF_PORT=3002
